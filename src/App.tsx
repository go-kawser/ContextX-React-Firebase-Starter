import React from "react";

const App: React.FC = () => {
  return (
    <div className="App">
      {/* The RouterProvider in main.tsx will handle the routing */}
    </div>
  );
};

export default App;
